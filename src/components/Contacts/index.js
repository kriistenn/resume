import React from 'react';
import Nav from '../Nav'
import style from './contacts.module.css'

function Contacts() {
    return (
    <div>
        <Nav />
        Contacts
    </div>
    );
}

export default Contacts;
